using System;
using System.Runtime.Remoting.Messaging;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace KomodoBadges_Tests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            // Triple A Paradigm (Arrange, Act, Assert)
            
            // Arrange
            // Make an instance of the repository
            // make an instance of the business object

            // Act
            // run the add method

            // Assert
            // check that the count of the list in the repo is greater than the initial count
            // check that the first business object in your "get all" method
            // is the object you just added 

           
        }
    }
}
